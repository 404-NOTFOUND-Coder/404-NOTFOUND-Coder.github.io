interface:
  display_name: "我给Agent的不是任务，是一张验收表"
  short_description: "把 Agent 任务改写成可检查的交付物、通过条件和停止边界。可直接复用的AI工作流模板。"
  default_prompt: "Use $agent-acceptance-sheet to process my material with this workflow."
